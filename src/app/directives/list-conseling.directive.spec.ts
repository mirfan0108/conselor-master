import { ListConselingDirective } from './list-conseling.directive';

describe('ListConselingDirective', () => {
  it('should create an instance', () => {
    const directive = new ListConselingDirective();
    expect(directive).toBeTruthy();
  });
});
