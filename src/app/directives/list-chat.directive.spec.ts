import { ListChatDirective } from './list-chat.directive';

describe('ListChatDirective', () => {
  it('should create an instance', () => {
    const directive = new ListChatDirective();
    expect(directive).toBeTruthy();
  });
});
