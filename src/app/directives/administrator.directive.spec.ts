import { AdministratorDirective } from './administrator.directive';

describe('AdministratorDirective', () => {
  it('should create an instance', () => {
    const directive = new AdministratorDirective();
    expect(directive).toBeTruthy();
  });
});
