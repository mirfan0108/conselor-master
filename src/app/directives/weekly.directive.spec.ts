import { WeeklyDirective } from './weekly.directive';

describe('WeeklyDirective', () => {
  it('should create an instance', () => {
    const directive = new WeeklyDirective();
    expect(directive).toBeTruthy();
  });
});
